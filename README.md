# Multi-Object-Tracking--PRCV-Final-Project
The system detects moving objects in video, assigns persistent identities across frames using a SORT-based tracker (Kalman filter + Hungarian algorithm), and renders color-coded trajectory trails along with analytics like speed estimation and collision detection.
